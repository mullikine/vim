diff -r 24d8ac7d92fc src/search.c
--- a/src/search.c	Wed May 07 21:14:47 2014 +0200
+++ b/src/search.c	Thu May 08 10:14:28 2014 +0100
@@ -2769,7 +2769,7 @@
 {
     char_u	*s;
 
-    s = ml_get(lnum);
+    s = skipwhite(ml_get(lnum));
     if (*s == para || *s == '\f' || (both && *s == '}'))
 	return TRUE;
     if (*s == '.' && (inmacro(p_sections, s + 1) ||
